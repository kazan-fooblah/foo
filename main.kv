#:kivy 1.0.9
<UI>:    
    canvas:
        Rotate:
            angle: root.angle
            origin: self.center
        Rectangle:
            pos: self.center_x - 5, 0
            size: 10, self.height